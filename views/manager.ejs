<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Панель менеджера</title>
</head>
<body>
    <div class="container">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
            <h1>Панель менеджера</h1>
            <div>
                <span>Вы вошли как: <strong><%= user.username %></strong> (<%= user.Role.name %>)</span>
                <a href="/logout" class="btn btn-danger" style="margin-left: 1rem;">Выйти</a>
            </div>
        </div>

        <div class="buttons">
            <a href="/" class="btn btn-primary">Главная страница</a>
            <% if (user.Role.name === 'admin') { %>
                <a href="/admin" class="btn btn-warning">Админ панель</a>
            <% } %>
        </div>

        <div style="background: #f8f9fa; padding: 2rem; border-radius: 6px; margin-top: 2rem;">
            <h2>Доступные действия для менеджера:</h2>
            <ul style="margin-top: 1rem;">
                <li>Просмотр всех продуктов</li>
                <li>Добавление/редактирование продуктов</li>
                <li>Управление поставщиками</li>
            </ul>
        </div>
    </div>
        <div style="margin-top: 2rem;">
        <h2>Управление продуктами</h2>
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Название</th>
                        <th>Цена</th>
                        <th>Категория</th>
                        <th>Поставщик</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(product => { %>
                    <tr>
                        <td><%= product.name %></td>
                        <td>$<%= product.price.toFixed(2) %></td>
                        <td><%= product.Category.name %></td>
                        <td><%= product.Supplier.name %></td>
                        <td class="actions">
                            <a href="/edit-product/<%= product.id %>" class="btn btn-primary">Изменить</a>
                            <form action="/delete-product/<%= product.id %>" method="POST">
                                <button type="submit" class="btn btn-danger">Удалить</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>